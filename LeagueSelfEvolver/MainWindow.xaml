<Window x:Class="LeagueSelfEvolver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LeagueSelfEvolver"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <XmlDataProvider x:Key="EventListData" Source="/DataModel/EventListData.xml" XPath="EventList" />
    </Window.Resources>
    <Grid>
        <DataGrid x:Name="dataGrid" HorizontalAlignment="Left" Margin="35,135,0,0" VerticalAlignment="Top" Height="115" Width="420" ItemsSource="{Binding Source={StaticResource EventListData}, Mode=TwoWay, XPath=Event }" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Goal Comment" Binding="{Binding XPath=GoalComment}" />
                <DataGridTextColumn Header="General Comment" Binding="{Binding XPath=GeneralComment}" />
                <DataGridTextColumn Header="Resolution" Binding="{Binding XPath=Tags}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
